paths:
  /pets:
    get:
      parameters:
        - name: tag
          in: query
          schema:
            type: array
            items:
              type: integer
              format: int64
      responses:
        '200': {}
        default: {}
