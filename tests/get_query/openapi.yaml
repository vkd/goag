paths:
  /pets:
    get:
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            format: int32
      responses:
        '200': {}
        default: {}
