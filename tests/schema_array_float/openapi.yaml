paths:
  /pets/ids:
    get:
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  type: number
